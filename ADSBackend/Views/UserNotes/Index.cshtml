@model IEnumerable<ADSBackend.Models.UserNotes>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.noteName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.noteContent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.noteName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.noteContent)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserNotesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserNotesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserNotesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
